# `components/`

UI reutilizable de Next.js (App Router): layouts, cards de postura, formularios de encuesta, etc. Preferir componentes de servidor salvo que haga falta estado cliente o streaming en el navegador.
